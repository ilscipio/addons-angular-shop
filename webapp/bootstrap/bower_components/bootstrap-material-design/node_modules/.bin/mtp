#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  "$basedir/../spacejam/bin/mtp" "$@"
  ret=$?
else 
  /bin/bash  "$basedir/../spacejam/bin/mtp" "$@"
  ret=$?
fi
exit $ret
