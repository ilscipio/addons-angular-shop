#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../spacejam/bin/unset-meteor-env" "$@"
  ret=$?
else 
  bash  "$basedir/../spacejam/bin/unset-meteor-env" "$@"
  ret=$?
fi
exit $ret
