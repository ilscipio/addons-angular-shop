#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  -xe "$basedir/../spacejam/bin/mpublish" "$@"
  ret=$?
else 
  /bin/bash  -xe "$basedir/../spacejam/bin/mpublish" "$@"
  ret=$?
fi
exit $ret
