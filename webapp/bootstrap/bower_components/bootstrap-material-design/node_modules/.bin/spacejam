#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../spacejam/bin/spacejam" "$@"
  ret=$?
else 
  node  "$basedir/../spacejam/bin/spacejam" "$@"
  ret=$?
fi
exit $ret
