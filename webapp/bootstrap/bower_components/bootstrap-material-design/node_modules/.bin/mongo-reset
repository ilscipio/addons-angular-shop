#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  -e "$basedir/../spacejam/bin/mongo-reset" "$@"
  ret=$?
else 
  /bin/bash  -e "$basedir/../spacejam/bin/mongo-reset" "$@"
  ret=$?
fi
exit $ret
