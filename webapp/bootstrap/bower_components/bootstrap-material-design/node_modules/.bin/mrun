#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../spacejam/bin/mrun" "$@"
  ret=$?
else 
  bash  "$basedir/../spacejam/bin/mrun" "$@"
  ret=$?
fi
exit $ret
