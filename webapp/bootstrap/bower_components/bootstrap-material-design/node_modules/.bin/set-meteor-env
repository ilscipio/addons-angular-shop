#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/bash" ]; then
  "$basedir//bin/bash"  -x "$basedir/../spacejam/bin/set-meteor-env" "$@"
  ret=$?
else 
  /bin/bash  -x "$basedir/../spacejam/bin/set-meteor-env" "$@"
  ret=$?
fi
exit $ret
